# Contract check — 2026-09-15

A contract is a short file describing what one piece of an interface promises:
the choices it offers, the values it starts with, and the parts it draws. This
run compares every contract against the code it describes.

Checked **4** contracts: badge, button, input, sketchbook-cta.

## What was found

**Passed** means checked and nothing wrong. **Needs attention** means something
was found. **Could not check** means it did not run, and those count as open below.

| Check | Result | What was found | What would fix it |
|---|---|---|---|
| The file is written correctly | Passed | nothing | — |
| Options the component does not have | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| The list of choices matches | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| One starting value, not two | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| Every named value exists | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| Nothing typed by hand | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| The parts described are the parts rendered | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| Keyboard and screen-reader promises | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |
| One owner per styling rule | Could not check | every contract is still waiting on its code | This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it. |

## How close this is to clean

**8 of 9 lines are open**, counting the **8** that could not be checked.

- **Options the component does not have** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **The list of choices matches** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **One starting value, not two** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **Every named value exists** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **Nothing typed by hand** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **The parts described are the parts rendered** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **Keyboard and screen-reader promises** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.
- **One owner per styling rule** — This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.

## If you only do one thing

This cannot run until the code a contract describes exists. Write the component, or point the contract at the file that already holds it.

<details>
<summary>Details for anyone who wants them</summary>

| Code | Name | State |
|---|---|---|
| C0 | The file is written correctly | verified |
| C1 | Options the component does not have | not-examined |
| C2 | The list of choices matches | not-examined |
| C3 | One starting value, not two | not-examined |
| C4 | Every named value exists | not-examined |
| C5 | Nothing typed by hand | not-examined |
| C6 | The parts described are the parts rendered | not-examined |
| C7 | Keyboard and screen-reader promises | not-examined |
| C8 | One owner per styling rule | not-examined |

```
             `components/ui/button.tsx` is where the code WILL live, not where it is. C1-C8 cannot run without it, so they are reported N/A here, not skipped and not failed. Re-run this check once the file exists — it will either promote this contract past draft or report real drift.

[36m DRAFT[0m  input.contract.json
        [36m·[0m [C0] component source not found — expected for a draft
             `components/ui/input.tsx` is where the code WILL live, not where it is. C1-C8 cannot run without it, so they are reported N/A here, not skipped and not failed. Re-run this check once the file exists — it will either promote this contract past draft or report real drift.

[36m DRAFT[0m  sketchbook-cta.contract.json
        [36m·[0m [C0] component source not found — expected for a draft
             `app/(studio)/pocket-sketchbook/about/about-view.tsx` is where the code WILL live, not where it is. C1-C8 cannot run without it, so they are reported N/A here, not skipped and not failed. Re-run this check once the file exists — it will either promote this contract past draft or report real drift.


4 contracts checked — 0 passing, 0 drifted, 4 draft (awaiting code).
```
</details>

